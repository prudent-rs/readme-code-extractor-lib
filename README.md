# readme-code-extractor-core

Internal: `readme-code-extractor-core` is shared between `readme-code-extractor` and
`readme-code-extractor-proc`. Do not use other than with `readme-code-extractor`'s macros.

```rust
# #[allow(unused)]
# fn hidden() {}
hidden();
```
